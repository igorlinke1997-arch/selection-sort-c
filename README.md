Selection Sort em C

 O que o programa faz

O código percorre um vetor e sempre seleciona o menor elemento da parte não ordenada, colocando-o na posição correta.


Compile com GCC:

```bash id="sel02"
gcc selection.c -o selection

executa:
Antes: 64 25 12 22 11
Depois: 11 12 22 25 64
